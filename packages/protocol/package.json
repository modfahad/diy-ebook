{
  "name": "@quran-device/protocol",
  "version": "0.1.0",
  "description": "Wire protocol shared by the desktop application and the device firmware",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/",
    "fixture": "npm run build && node dist/scripts/build-transcript.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5"
  }
}
