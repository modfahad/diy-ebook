# Shared packages

**Status: `qpk-format` and `protocol` implemented (Milestones 2 and 4); `shared-types` deliberately empty.**

Code shared between the desktop app, tools and (by mirrored definition) the firmware.
